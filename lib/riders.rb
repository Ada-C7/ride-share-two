module RideShare
  class Riders

    def find_trips(id)
      #given rider_id find a list of trips
      #also return the driver instances for each trip
      #RideShare::Trips.find_rider_trips
    end

    def self.all
      #find all riders
    end

    def find(id)
      #given rider_id, return rider instance
    end

  end
end
