
module RideShare
  class Trip


    def find_many(id)
      # driver_trips = []
      # trips = self.all
      #
      # trips.each do |trip|
      #   if driver_id == id
      #     driver_trips << trip
      #     return driver_trips
      #   end
      # end
    end

  end
end
