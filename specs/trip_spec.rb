

# TODO: Make sure array is picking values correctly.

require_relative 'spec_helper.rb'
#
# describe "retrieve an array from self.all" do
#   before do
#     # TODO: update to let()
#     @trip = RideShare::Trip.all
#   end
#
#   it "retrieve an array" do
#     skip
#     @trip.must_be_instance_of Array
#   end
#
#   it "retrieves trip_id" do
#     skip
#     CSV.read('support/drivers.csv') do |line|
#       counter = 0
#       @trip[counter].trip_id.must_equal line[counter][0].to_i
#       puts @trip[counter]
#       counter += 1
#     end
#   end
#
#   it "trip_id, driver_id, rider_id, date, and rating match the first and last element" do
#     skip
#     # @trip.first.trips_array[0][0].must_equal 1
#     # @trip.first.driver_id.must_equal 1
#     # @trip.first.rider_id.must_equal 54
#     # @trip.first.rider_id.must_equal "2016-04-05"
#     # @trip.first.rating.must_equal 3
#   end
#
#   it "rating is within acceptable range" do
#     skip
#
#   end
#
# end
